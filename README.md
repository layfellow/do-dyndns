# Set dynamic DNS IP for Digital Ocean

## Update all dependencies

Run:

    $ make dependencies

## Build the binary

Run:

    $ make build

## Install the binary in your default path

Run:

    $ make install
